# Copy to .env and fill in. Never commit .env.
STRIPE_SECRET_KEY=sk_live_xxxxxxxx
# Used for Stripe Checkout return URLs when API and site are on different hosts
PUBLIC_SITE_URL=https://www.hygienefirst.com
